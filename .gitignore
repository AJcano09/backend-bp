# Environment and local tooling (never commit secrets or personal config)
# .env files are tracked with safe defaults (changeme/guest/localhost) so
# the evaluator can run docker compose up --build immediately after clone.
# Override locally with real credentials if needed — never commit those.
.atl/
.config/
.idea/
*.DotSettings
*.DotSettings.user

# Build output
bin/
obj/